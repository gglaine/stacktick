<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>Searching for - <%= @search_term %></h1>
      <% @courses.each do |course| %>
        <div class="card-course">
          <h4>course slug :  <%= course["slug"] %></h4>
          <h4><%= course["name"] %></h4>
        </div>
      <% end %>
    </div>
  </div>
</div>

